<%= simple_form_for [@offer, @booking] do |f| %>
  <%= f.input :date, as: :date %>
  <%= f.input :reason %>
  <%= f.submit 'Book now', class: 'btn btn-warning my-3 px-3', style: 'border-radius: 20px' %>
<% end %>
